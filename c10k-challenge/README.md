This folder builds and tests a C10K-capable HTTP server: the server itself
(not written yet) and the load-test harness it will be validated against
both live here.

See `TESTING_PLAN.md` for the harness design and `tests/README.md` for how
to build and run each tool.
