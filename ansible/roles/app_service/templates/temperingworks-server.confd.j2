# Managed by Ansible (roles/app_service).
# rc_ulimit is OpenRC's no-PAM mechanism for per-service ulimits — this filename must
# exactly match /etc/init.d/{{ service_name }} or OpenRC silently ignores it.
rc_ulimit="-n 65535"
APP_PORT={{ app_http_port }}
