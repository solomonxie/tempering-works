# Managed by Ansible (roles/logging). The server writes its own log files directly
# (not syslog), so this is what actually caps their size/age.
{{ app_log_dir }}/*.log {
    daily
    rotate 7
    compress
    missingok
    notifempty
    copytruncate
}
